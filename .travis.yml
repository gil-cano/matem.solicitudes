language: python
python:
  - 2.7
cache:
  directories:
    - eggs
install:
  - python bootstrap-buildout.py -c travis.cfg
  - bin/buildout -Nc travis.cfg
script:
  - bin/test
after_success:
  - bin/createcoverage
  - pip install coveralls
  - coveralls
